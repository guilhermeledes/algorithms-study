/*
 * Number of Matching Subsequences
 *
 * This file is intentionally scaffolded on `main`.
 * Use the corresponding `solve/number-of-matching-subsequences` worktree for the completed implementation.
 */

const notImplementedMessage =
  "Not implemented on main; use the solve/number-of-matching-subsequences worktree.";

export function countMatchingSubsequencesBrute(s: string, words: string[]): number {
  void s;
  void words;
  throw new Error(notImplementedMessage);
}

export function countMatchingSubsequencesOptimized(s: string, words: string[]): number {
  void s;
  void words;
  throw new Error(notImplementedMessage);
}

export function countMatchingSubsequencesVideoStyle(s: string, words: string[]): number {
  void s;
  void words;
  throw new Error(notImplementedMessage);
}

export function countMatchingSubsequencesTwoPointers(s: string, words: string[]): number {
  void s;
  void words;
  throw new Error(notImplementedMessage);
}
