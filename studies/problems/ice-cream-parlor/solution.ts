/*
 * Ice Cream Parlor
 *
 * This file is intentionally scaffolded on `main`.
 * Use the corresponding `solve/ice-cream-parlor` worktree for the completed implementation.
 */

const notImplementedMessage =
  "Not implemented on main; use the solve/ice-cream-parlor worktree.";

export function iceCreamParlorBrute(money: number, prices: number[]): number[] {
  void money;
  void prices;
  throw new Error(notImplementedMessage);
}

export function iceCreamParlorOptimized(money: number, prices: number[]): number[] {
  void money;
  void prices;
  throw new Error(notImplementedMessage);
}

export function icecreamParlor(m: number, arr: number[]): number[] {
  void m;
  void arr;
  throw new Error(notImplementedMessage);
}
