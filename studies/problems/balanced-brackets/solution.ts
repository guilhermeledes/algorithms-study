/*
 * Balanced Brackets
 *
 * This file is intentionally scaffolded on `main`.
 * Use the corresponding `solve/balanced-brackets` worktree for the completed implementation.
 */

const notImplementedMessage =
  "Not implemented on main; use the solve/balanced-brackets worktree.";

export function isBalancedBruteForce(s: string): string {
  void s;
  throw new Error(notImplementedMessage);
}

export function isBalancedOptimized(s: string): string {
  void s;
  throw new Error(notImplementedMessage);
}

export function isBalanced(s: string): string {
  void s;
  throw new Error(notImplementedMessage);
}
