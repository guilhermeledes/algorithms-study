/*
 * Remove Islands
 *
 * This file is intentionally scaffolded on `main`.
 * Use the corresponding `solve/remove-islands` worktree for the completed implementation.
 */

const notImplementedMessage =
  "Not implemented on main; use the solve/remove-islands worktree.";

export type BinaryMatrix = number[][];

export function removeIslandsBrute(matrix: BinaryMatrix): BinaryMatrix {
  void matrix;
  throw new Error(notImplementedMessage);
}

export function removeIslandsOptimized(matrix: BinaryMatrix): BinaryMatrix {
  void matrix;
  throw new Error(notImplementedMessage);
}
